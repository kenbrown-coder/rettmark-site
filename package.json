{
  "name": "rettmark-site",
  "private": true,
  "description": "Static site + Netlify Functions (Authorize.Net checkout)",
  "scripts": {
    "build": "node scripts/generate-sitemap.cjs && node scripts/inject-google-site-verification.cjs && node scripts/inject-turnstile-config.cjs && node scripts/inject-anet-config.cjs"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.4"
  }
}
