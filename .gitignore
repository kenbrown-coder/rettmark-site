# Local deploy bundle (regenerate anytime; large)
rettmark-site-netlify.zip
inventory_sheet_seed.csv

# Python
__pycache__/
*.py[cod]
.Python

# Node (Netlify runs npm install when package.json exists)
node_modules/

# Local secret overrides (keep real keys here; never commit)
.env.local
.env.*.local

# Editor / OS
.vscode/preview-url.txt
.cursor/
.DS_Store
Thumbs.db
